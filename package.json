{
  "name": "pi-hub",
  "version": "0.1.0",
  "description": "Pi HUD - A status bar extension for pi-coding-agent, inspired by claude-hud (formerly jimpi)",
  "private": true,
  "scripts": {
    "check": "tsc --noEmit --skipLibCheck extensions/pi-hud.ts",
    "install:local": "node scripts/install.js",
    "install:global": "node scripts/install.js --global"
  },
  "keywords": ["pi", "pi-coding-agent", "hud", "statusbar", "extension"],
  "license": "MIT"
}
